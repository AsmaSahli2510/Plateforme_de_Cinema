# CinéEvent exact React + Vite version

This version follows your provided HTML much more closely.

## Start
```bash
npm install
npm run dev
```

## Important
The original HTML uses these local image paths:
- /images/upscalemedia-transformed.png
- /images/Gemini_Generated_Image_8huwzw8huwzw8huw.png

I added placeholder image files in `public/images/` so the project runs immediately.
To make it look exactly like your screenshot/source, replace those two files with your original images using the same filenames.
